<?php
class VirtualUser extends \Illuminate\Database\Eloquent\Model
{
  // Eloquent Model class finds the database by himself (plural of Wish)
  // protected $table = 'virtual_users';
  // Writable column list
  // protected $fillable = ['message', 'signature'];
  public $timestamps = false;
}
